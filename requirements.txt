# Core data & math
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.13.0
scikit-learn>=1.4.0   # ML signal filter (src/modules/signal_filter.py)

# Exchange connectivity
ccxt>=4.3.0
# Used directly by notifications/telegram.py, data/futures_metrics.py and
# data/positioning.py. It has always arrived transitively via ccxt; declared
# explicitly so a future dependency change cannot silently break the bot.
requests>=2.31.0

# Dashboard
streamlit>=1.35.0
plotly>=5.22.0

# Data storage
pyarrow>=16.0.0   # parquet support for pandas

# Testing
pytest>=8.2.0
pytest-cov>=5.0.0

# Utilities
python-dateutil>=2.9.0
